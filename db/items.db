CREATE TABLE sqlite_sequence(name,seq);
CREATE TABLE categories (category_id INTEGER PRIMARY KEY, category_name TEXT UNIQUE);
CREATE TABLE items(id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL, name TEXT, category_id TEXT, image_name TEXT, foreign key (category_id) references categories(category_id));